import actions from './actions';
import mutations from './mutations';

const restaurant = {
   state:{
      restList: [],
      restName: [],
      tags: []
   },
   actions,
   mutations
};

export default restaurant;